import { paySauceColor } from 'src/constants/Colors';

export const DEFAULT_SIZE = 32;
export const DEFAULT_DOT_COLOR = paySauceColor.hotChilli;
export const DEFAULT_FORWARD_COLOR = paySauceColor.lightGrey;
